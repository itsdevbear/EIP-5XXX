{
  "name": "forge-template",
  "author": "FrankieIsLost",
  "version": "1.0.0",
  "description": "A forge template",
  "homepage": "https://github.com/FrankieIsLost/forge-template#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FrankieIsLost/forge-template.git"
  },
  "scripts": {
    "format": "yarn run prettier --write src/**/*.sol --config config/prettier.json --ignore-path config/.prettierignore",
    "lint": "yarn run prettier --check src/**/*.sol --config config/prettier.json --ignore-path config/.prettierignore && yarn run solhint --config ./config/solhint.json --ignore-path ./config/.solhintignore 'src/**/*.sol'"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "prettier-plugin-solidity": "^1.0.0-beta.19",
    "solhint": "^3.3.6"
  }
}